<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền của EditText -->
    <solid android:color="#595959"/>   <!-- ← đổi chỗ này -->

    <!-- Bo góc -->
    <corners android:radius="10dp"/>

    <!-- Viền -->
    <stroke android:width="1dp" android:color="#555555"/>

    <!-- Khoảng cách trong -->
    <padding
        android:left="10dp"
        android:top="8dp"
        android:right="10dp"
        android:bottom="8dp"/>
</shape>
